/// <reference types="cypress" />

// Shared Cypress commands live here. Task-specific helpers are added by the change that needs
// them, so this file stays empty until something is genuinely reused across specs.

export {};
